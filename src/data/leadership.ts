import { LeadershipMember } from '@/lib/types';

/**
 * Leadership data is loaded dynamically from the PostgreSQL database via `/api/leadership`.
 * This empty array serves as a fallback type reference.
 */
